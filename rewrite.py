import re

FILE_PATH = r"c:\Users\ADMIN\Desktop\Agent-2\new_agent_2_BE\agents\risk_evaluation_agent.py"

with open(FILE_PATH, "r", encoding="utf-8") as f:
    code = f.read()

# We'll just replace the RiskEvaluationAgent class with a new structure.
# But wait, there is too much logic to rewrite accurately in a script.
# Let's just create the Session class.
